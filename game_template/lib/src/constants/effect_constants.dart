class EffectConstants {
  /// Duration of movement effects, per matrix unit.
  static const double movementDuration = 0.1;

  /// Duration of the scale effect when the ingredient is held.
  static const double scaleDuration = 0.1;

  /// Duration of the scale effect when the ingredient is held.
  static const double fadeDuration = 0.1;

  /// Duration of the scale effect when the ingredient is held.
  static const double fadeDelay = 1;

  /// Scale of the object when held.
  static const double heldScale = 1.1;
}
